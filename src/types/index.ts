export type StringType = string;

export type FeatureInfoType = {
    id: number;
    listNumber: string;
    title: string;
    desc: string;
}

export type FooterIconsType = {
    id: number;
    icon: string;
    alt: string;
}